to add modified files
	git add -u :/
to commit files
	git commit -m "comment"
to add branch
	git checkout -b newbranch
to push branch to github
	git push origin newbranch
to check git status of all tracking files
	git status -uno
to check current working branch
	git branch

post build script
	../poky/meta-intel-edison/utils/flash/postBuild.sh

linux-externalsrc local git mirror locates at
	build_edison/downloads/git2/github.com.Anteater-GitHub.edison-linux.git
to push linux-externalsrc commits to remote server
	git push https://github.com/Anteater-GitHub/edison-linux.git
the local git clone is at 
	../poky/linux-kernel/
Can commit on local linux clone to local git, then push the commits to remote git

move head back to previous commit	
	git reset --hard commitID
from local git push to remote (roll back commit)
	git push -f origin edison-3.10.98
get the latest repo
	git pull 
